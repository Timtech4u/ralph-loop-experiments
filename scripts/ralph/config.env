# Ralph Configuration
# Edit these settings before running ralph.sh

# Review backends: rp (RepoPrompt), codex (OpenAI Codex CLI), none
PLAN_REVIEW=codex
WORK_REVIEW=codex

# Require plan review before work starts (1 = yes, 0 = no)
REQUIRE_PLAN_REVIEW=1

# Branch mode: new (create ralph-<run-id> branch), current, worktree
BRANCH_MODE=new

# Limits
MAX_ITERATIONS=25
MAX_TURNS=
MAX_ATTEMPTS_PER_TASK=5
WORKER_TIMEOUT=1800

# Scope: limit to specific epics (comma-separated, empty = all)
EPICS=

# Permissions: 1 = skip permission prompts (required for unattended)
YOLO=1

# UI: 1 = colored output, 0 = plain
RALPH_UI=1
